class ExperienceClass {
  final int index;
  final String expTitle;
  final String jobTitle;
  final String imagePath;
  final DateTime startDate;
  final DateTime endDate;
  final bool hasBackground;
  final String description;
  ExperienceClass(this.index, this.expTitle, this.jobTitle, this.imagePath, this.startDate, this.endDate, this.hasBackground, this.description);
}