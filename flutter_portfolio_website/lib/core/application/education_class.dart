class EducationClass {
  final int index;
  final String eduTitle;
  final String courseTitle;
  final DateTime startDate;
  final DateTime endDate;
  final String grade;
  final String description;
  final String imagePath;
  final bool hasBackground;
  EducationClass(this.index, this.eduTitle, this.courseTitle, this.startDate, this.endDate, this.grade, this.description, this.imagePath, this.hasBackground);
}